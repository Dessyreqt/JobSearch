{
  "ConnectionString": "Server=localhost;Database=JobSearch.Tests;Trusted_Connection=True;", // for testing, this should just run against localhost
  "DapperIdentityCryptography": {
    "Key": "", // this should be a guid (minus separators) encoded with base64
    "IV": "" // this should be a random string encoded with base64
  }
}